export { Component as default } from "./route.component";
// social-posts/:socialPostId — the post detail page (top-level resource URL).
// Read-only: loader fetches the post + media + per-account results.
export { loader } from "./route.loader";
