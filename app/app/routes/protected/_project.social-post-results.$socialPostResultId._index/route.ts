export { Component as default } from "./route.component";
// social-post-results/:socialPostResultId — the standalone result page (a
// top-level resource URL). Read-only: loader fetches the result, its account,
// the resolved configuration, and the raw provider response.
export { loader } from "./route.loader";
