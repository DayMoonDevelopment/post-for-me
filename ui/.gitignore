.DS_Store
/node_modules/

# Env — never commit real keys (PostHog etc.). Only .env.example is tracked.
.env
.env.local
.env.*.local

# Claude Code — machine-specific local settings (local paths, personal MCP/permissions).
.claude/settings.local.json

# React Router
/.react-router/
/build/

# Vercel
/.vercel/
